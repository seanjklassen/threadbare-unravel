# Windows Installer Assets

Place branding assets here.

- `wizard-image.png` (recommended 240x459 or higher for HiDPI)
- `wizard-small.png` (recommended 55x58)

These paths are referenced in `installer/product.json` and `Installer.iss`.
